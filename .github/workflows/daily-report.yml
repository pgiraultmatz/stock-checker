name: Daily Stock Report

on:
  schedule:
    # Run at 5:00 UTC every day
    - cron: '0 5 * * *'
  workflow_dispatch:

env:
  GO_VERSION: '1.22'

jobs:
  stock-report:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Build application
        run: |
          echo "🔨 Building stock-checker..."
          go build -o stock-checker ./cmd/stock-checker
          echo "✅ Build complete"

      - name: Set date
        id: date
        run: |
          echo "TODAY=$(date +'%d/%m/%Y')" >> $GITHUB_OUTPUT
          echo "TODAY_SAFE=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Run stock analysis
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TWITTER_USERNAMES: ${{ secrets.TWITTER_USERNAMES }}
        run: |
          echo "📊 Generating stock market report..."
          ./stock-checker -config config.json -output report.html -verbose
          echo "✅ HTML report generated"

      - name: Send email report
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "Stock Market Report - ${{ steps.date.outputs.TODAY }}"
          html_body: file://report.html
          to: ${{ secrets.EMAIL_RECIPIENT }}
          from: Stock Analyzer <${{ secrets.EMAIL_USERNAME }}>
          secure: true

      - name: Upload report artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: stock-report-${{ steps.date.outputs.TODAY_SAFE }}
          path: report.html
          retention-days: 30
